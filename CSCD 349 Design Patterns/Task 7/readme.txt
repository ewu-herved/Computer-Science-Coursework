Dan Herve
CSCD 349
Task 7

The methods all work and produce expected results. The only thing I was unable to 
figure out was how do reduce the list size of the stored words while maintaining the
given design. With no parameters being passed into Links and no methods to change the indeces,
removing nodes from the list of Words in WordPool would have thrown off all of my index pointers in
the Link objects. As such, I removed words by replacing them with empty strings (i.e. ""). 